[project]
name = "softmax"
version = "0.1.0"
dependencies = [
    "matplotlib",
    "numpy",
    "tinygrad",
]

[project.optional-dependencies]
dev = [
    "pre-commit",
]

[tool.ruff.lint]
extend-select = ["I"]
